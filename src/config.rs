// Path for storing all uploaded images
pub const IMG_STORAGE_PATH: &str = "./img-test-storage";

// Server & port
pub const SERVER: (&str, u16) = ("127.0.0.1", 8080);

// Image upload
pub const MAX_FILE_SIZE: usize = 1024 * 1024 * 10; // 10 MB
